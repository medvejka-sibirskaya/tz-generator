# TZ Generator — генератор технических заданий

## Что это

Финальный проект курса «Вайб-кодинг на Claude Code» (ZeroCoder, CLm04) и монетизируемый продукт. Веб-сервис: пользователь отвечает на вопросы → генератор выдаёт готовое ТЗ.

## Описание проекта (структура школы, 7 пунктов)

1. **Название:** Генератор технических заданий (TZ Generator)
2. **Проблема:** фрилансеры и малые команды пишут ТЗ вручную — долго, без структуры, отсюда споры с заказчиком о объёме работ.
3. **Пользователь:** фрилансеры, студии 1–3 человек, заказчики, формулирующие задачу подрядчику.
4. **Основная функция:** веб-страница с формой-опросником → генерация готового ТЗ. Два режима:
   - **Облегчённый** — бесплатно, минимальная структура;
   - **По ГОСТ (19/34)** — 100 ₽, полная структура: стадии, требования, разделы. Ядро — методичка/шаблон по ГОСТ (сопутствующий скилл tz-gost).
5. **Формат результата:** готовое ТЗ на экране + скачивание .md / .docx.
6. **Минимальный функционал (MVP):** форма → облегчённое ТЗ; режим ГОСТ; скачивание файла; живая оплата ЮMoney для ГОСТ-режима.
7. **Технологии:** Python, Flask, HTML/CSS, шаблоны ТЗ, генерация текста через LLM API (ключи — только в env, не в коде).

## Монетизация (правило Медвежки с 21.09)

- Все проекты оцениваются по деньгам/репутации. Схема здесь: облегчённый бесплатно (привлечение) + ГОСТ за 100 ₽ (доход).
- Оплата **живая** через ЮMoney (как в уроке NS06.1 n8n-курса). Секреты платежей — в env, не в репозитории.

## Деплой

- Продакшн: поддомен **tz.medvejka.ru** (сервер Медвежки, Dokploy — как medvejka.ru). Контейнер отдельный, основной сайт не трогать.
- Любой деплой и публикация на GitHub — только по явному «заливай» Медвежки.

## Дедлайны и сдача

- На проект — 2 недели с 21.09.2026 (онлайн-выпускной, защита 3–5 мин). **Деплой готов 24.09.**
- Сдать в школу: ссылку на GitHub-репозиторий (README, скриншоты, скилл `.claude/`), анкету выпускного.
- Скилл проекта: промпт из урока CLm04 (см. конспект `F:\Конспекты\Claude\Итоговый проект.md`) — **ещё не сделан**.
- Осталось: скилл `.claude/` → живой тест оплаты 100 ₽ на проде → HTTP-уведомления ЮMoney (закрыть доверие к /gost/paid) → анкета выпускного.

## Правила проекта

- Имена файлов и папок — латиница (кириллица ломает Claude Code и git).
- git commit свободно, push — только по «заливай».
- Коммиты с `Co-Authored-By: Claude`.
- Скриншоты для README делаем по ходу разработки, сжимать перед заливом (squoosh/tinypng).

## Изменения и планы

- **21.09.2026:** старт. Создана папка, git init, CLAUDE.md. Дальше: каркас Flask → облегчённый режим → ГОСТ-режим → ЮMoney → деплой на tz.medvejka.ru.
- **24.09.2026:** весь код MVP написан (оба режима, ИИ-полировка через YandexGPT, .md/.docx, SEO, ЮMoney). Протестировано локально: все страницы и флоу работают. Первый коммит `0b58ed0` (+ фиксы gitignore). Подключён живой кошелёк ЮMoney (YOOMONEY_RECEIVER в .env), оплата работает.
- **Чеки (самозанятость):** сервис ЮSelf отключён с 26.12.2025 — чеки выбивать вручную в приложении «Мой налог» (штраф за нечек 20% суммы). Авточеки (Robokassa/CKassa, ~3,5–4%) — в бэклог.
- **Дальше:** QR-код оплаты на странице /gost/pay → README + скрины → скилл .claude/ → деплой на tz.medvejka.ru (по «заливай»).
- **24.09, деплой:** проект задеплоен на Dokploy → **https://tz.medvejka.ru** (порт контейнера 5001, HTTPS). Репозиторий: github.com/medvejka-sibirskaya/tz-generator (ветка main). Механизм оплаты (`payments.py`) и Telegram-уведомления (`notify.py`) — приватные локальные модули, в репозиторий и docker-образ НЕ входят (в .gitignore и .dockerignore); без них app.py работает: оплата = заглушка, уведомления отключены. История коммитов почищена (один чистый коммит на main, старая история — в локальной ветке backup-full-history). Dockerfile + gunicorn + .dockerignore добавлены.